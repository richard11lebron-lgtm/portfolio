# Instructions

This repo is for a portfolio site for Richie Lebron. He is non-technical so make sure to explain things in simple language for him.

The code needs to all live in the `site/` subfolder.

Automatically handle commits and pushing up code changes.

The site is already set up on `Cloudflare Pages` with the domain `richielebron.com` and NONE of that needs to be done.

The site must remain strictly static with only html, css, and js. Nothing more than a static site ever. If it ever needs to go in that direction tell him to contact `Jorge`.